<%= bootstrap_form_with(model: offer, local: true) do |form| %>
  <% if offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field login_field">
    <%= form.text_field :title %>
  </div>
  
  <div class="field login_field">
    <%= form.text_area :content %>
  </div>

  <div class="field login_field">
    <%= form.text_field :price %>
  </div>

  <div class="field login_field">
    <%= form.number_field :time %>
  </div>

  <div class="field">
    <%= form.label :in_person %>
    <%= form.check_box :in_person, { hide_label: true }, "yes", "no" %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
